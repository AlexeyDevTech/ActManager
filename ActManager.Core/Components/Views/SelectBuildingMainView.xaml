<UserControl x:Class="ActManager.Core.Components.Views.SelectBuildingMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:ActManager.Core.Components.ViewModels"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance vm:SelectBuildingMainViewModel}"
             d:DesignWidth="1200" d:DesignHeight="900">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ActManager.Core;component/Resources/StyleDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
        
    </UserControl.Resources>
    <Grid Background="#FF434E5C">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="1"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="15 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Сведения об объекте:" VerticalAlignment="Center" FontSize="24"/>
            <Grid Grid.Column="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Редактировать" HorizontalAlignment="Right" Margin="5" FontSize="14"/>
                <Button Grid.Column="1" Content="Удалить" HorizontalAlignment="Right" Margin="5" FontSize="14"/>
            </Grid>
            
        </Grid>
        <Border Grid.Row="1" Height="1" BorderThickness="1" BorderBrush="#CCC" Margin="3,0,20,0" />
        <StackPanel Grid.Row="2" Orientation="Vertical">
            <Grid Height="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Имя Объекта:" Margin="10 0" FontSize="22" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding Name}" Margin="10 0" FontSize="22" VerticalAlignment="Center"/>
            </Grid>
            
            <Grid Height="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Адрес Объекта:" Margin="10 0" FontSize="22" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding Address}" Margin="10 0" FontSize="22" VerticalAlignment="Center"/>
            </Grid>
            
            <Grid Height="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Офис:" Margin="10 0" FontSize="22" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding OfficeNum}" Margin="10 0" FontSize="22" VerticalAlignment="Center"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
