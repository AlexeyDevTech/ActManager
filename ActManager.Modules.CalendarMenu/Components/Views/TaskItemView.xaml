<UserControl x:Class="ActManager.Modules.CalendarMenu.Components.Views.TaskItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ActManager.Modules.CalendarMenu.Components.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:core="clr-namespace:ActManager.Core;assembly=ActManager.Core"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/">
    <Grid>
        <Border Background="White" Height="100" CornerRadius="16" Margin="0 0 0 20" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Color="#EEE" ShadowDepth="5" Direction="270"></DropShadowEffect>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="{Binding Title}" FontFamily="Verdana" FontWeight="Bold" FontSize="20" />
                <TextBlock Grid.Row="1" TextWrapping="Wrap" Text="{Binding Description}" FontFamily="Verdana" FontSize="14"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
