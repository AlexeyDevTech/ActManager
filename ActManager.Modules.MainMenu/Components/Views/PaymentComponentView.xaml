<UserControl
    x:Class="ActManager.Modules.MainMenu.Components.Views.PaymentComponentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:ActManager.Modules.MainMenu.Components.Converters"
    xmlns:core="clr-namespace:ActManager.Core;assembly=ActManager.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:ActManager.Modules.MainMenu.Components.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:PaymentComponentViewModel}"
    d:DesignHeight="250"
    d:DesignWidth="1704"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conv:StatusToBrushConverter x:Key="StatusToBrushConverter" />
        <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Border
            Grid.Row="1"
            Grid.Column="1"
            Grid.ColumnSpan="3"
            Margin="10,0"
            Background="White"
            CornerRadius="20">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="80" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="Tahoma"
                        FontSize="18"
                        FontWeight="Bold"
                        Foreground="#333"
                        Text="Предстоящие платежи" />
                    <TextBlock
                        Grid.Row="1"
                        VerticalAlignment="Center"
                        FontFamily="Tahoma"
                        FontSize="16"
                        Foreground="#999"
                        Text="Не забудьте о важном" />
                    <Button
                        Grid.Row="0"
                        Grid.RowSpan="2"
                        Grid.Column="4"
                        Height="30"
                        Command="{Binding CreateActCommand}">
                        <Button.Style>
                            <Style TargetType="{x:Type Button}">
                                <Setter Property="Background" Value="#FFE7F0FF" />

                                <Setter Property="Margin" Value="5" />
                                <Setter Property="BorderThickness" Value="0.5" />
                                <Setter Property="BorderBrush" Value="#CCC" />
                                <Setter Property="Opacity" Value="0.6" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border
                                                Padding="{TemplateBinding Padding}"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Opacity" Value="0.9" />
                                                    <Setter Property="BorderThickness" Value="1" />
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Opacity" Value="1" />
                                                    <Setter Property="Background" Value="#FFD3DCEB" />
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.2" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                        <Grid>
                            <TextBlock
                                FontFamily="Tahoma"
                                FontWeight="Bold"
                                Foreground="#FF465AB9"
                                Text="Составить акт платежа" />
                        </Grid>
                    </Button>
                    <Border
                        Grid.RowSpan="1"
                        Grid.Column="1"
                        Margin="5,0"
                        Padding="10,0"
                        HorizontalAlignment="Left"
                        Background="#FFF7BBB3"
                        CornerRadius="16">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding OverduePayments}" Value="0">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto" />
                                <ColumnDefinition Width="7" />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontFamily="Tahoma"
                                FontSize="15"
                                Foreground="#FFD5665D"
                                Text="{Binding OverduePayments}" />
                            <TextBlock
                                Grid.Column="2"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontFamily="Tahoma"
                                FontSize="15"
                                Foreground="#FFD5665D"
                                Text="просрочено" />
                        </Grid>
                    </Border>
                </Grid>
                <Grid Grid.Row="1">
                    <ScrollViewer>
                        <ItemsControl ItemsSource="{Binding Payments}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border
                                        Margin="5"
                                        Padding="10"
                                        Background="{Binding Status, Converter={StaticResource StatusToBrushConverter}, ConverterParameter=Оплачен:#E6FFE6;Обработка:#FFF3E6;Просрочен:#FFE6E6}"
                                        BorderBrush="Gray"
                                        BorderThickness="1"
                                        CornerRadius="5">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <StackPanel>
                                                <TextBlock
                                                    FontWeight="Bold"
                                                    Foreground="{Binding Status, Converter={StaticResource StatusToBrushConverter}, ConverterParameter=Оплачен:Green;Обработка:Orange;Просрочен:Red}"
                                                    Text="{Binding Amount, StringFormat='Сумма: {0:C}', ConverterCulture=RU-ru}" />
                                                <TextBlock Text="{Binding Status, StringFormat='Статус: {0}'}" />
                                                <TextBlock
                                                    FontSize="12"
                                                    Foreground="Gray"
                                                    Text="{Binding PaymentDate, StringFormat='Дата платежа: dd.MM.yyyy HH:mm'}" />
                                                <TextBlock
                                                    FontSize="12"
                                                    Foreground="Gray"
                                                    Text="{Binding DueDate, StringFormat='Срок: dd.MM.yyyy'}" />
                                                <TextBlock
                                                    FontSize="12"
                                                    Foreground="Gray"
                                                    Text="{Binding Source, StringFormat='Источник: {0}'}" />
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                                <TextBlock Text="{Binding Contract.Id, StringFormat='Контракт: {0}'}" />
                                                <TextBlock Text="{Binding BankTransaction.Id, StringFormat='Транзакция: {0}', FallbackValue='Нет транзакции'}" Visibility="{Binding BankTransaction, Converter={StaticResource NullToVisibilityConverter}}" />
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Grid>

        </Border>
    </Grid>
</UserControl>
