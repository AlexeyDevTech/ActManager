<UserControl x:Class="ActManager.Modules.Header.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ActManager.Modules.Header.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="68" d:DesignWidth="1599"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ActManager.Modules.Header;component/Resources/StyleDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="C0"/>
            <ColumnDefinition x:Name="C1"/>
            <ColumnDefinition x:Name="C2"/>
            <ColumnDefinition x:Name="C3"/>
            <ColumnDefinition x:Name="C4"/>
        </Grid.ColumnDefinitions>
        <Grid x:Name="stackPanel" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border Width="50" Height="50" Margin="15 0">
                <Grid>
                    <Image Source="/ActManager.Modules.Header;component/Resources/avatar.png" SnapsToDevicePixels="True"/>
                </Grid>
            </Border>
            <Grid Grid.Column="1">
                <TextBlock Text="{Binding UserName}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="10 0" Grid.IsSharedSizeScope="True"/>
            </Grid>
        </Grid>
        <Grid Grid.Column="4">
            <Button HorizontalAlignment="Right" Content="настройки" Height="25" Visibility="Hidden"/>
        </Grid>
    </Grid>
</UserControl>
