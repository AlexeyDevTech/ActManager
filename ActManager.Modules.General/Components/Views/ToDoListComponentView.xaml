<UserControl x:Class="ActManager.Modules.General.Components.Views.ToDoListComponentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:local="clr-namespace:ActManager.Modules.General.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:core="clr-namespace:ActManager.Core;assembly=ActManager.Core"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:ActManager.Modules.General.Components.ViewModels"
             xmlns:conv="clr-namespace:ActManager.Modules.General.Components.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vm:ToDoListComponentViewModel}"
             d:DesignHeight="316" d:DesignWidth="1380"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <conv:AlternationToBrushConverter x:Key="AlternationToBrushConverter"/>
    </UserControl.Resources>
    <ScrollViewer>
    <Grid d:Background="White">
    <Grid.RowDefinitions>
        <RowDefinition Height="25"/>
        <RowDefinition/>
    </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.2*"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="2.5*"/>
                    <ColumnDefinition Width="0.5*"/>
                    <ColumnDefinition Width="0.5*"/>
                </Grid.ColumnDefinitions>
                <!--<TextBlock Text="ID"/>-->
                <TextBlock Text="Задача" Grid.Column="1"/>
                <TextBlock Text="Описание" Grid.Column="2"/>
                <TextBlock Text="Статус" Grid.Column="3"/>
                <TextBlock Grid.Column="4"/>
            </Grid>
    <ItemsControl Grid.Row="1" x:Name="ToDoListControl" ItemsSource="{Binding Goals}" AlternationCount="2">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="ContentControl.Background" Value="White"/>
                    <Setter Property="ContentControl.FontSize" Value="16"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0"  Height="40" Name="ListEl">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.2*"/>
                            <ColumnDefinition />
                            <ColumnDefinition Width="2.5*"/>
                            <ColumnDefinition Width="0.5*"/>
                            <ColumnDefinition Width="0.5*"/>
                        </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" BorderBrush="#999" BorderThickness="0 0 0.5 0">
                                <TextBlock VerticalAlignment="Center" TextAlignment="Center" Text="{Binding ID}"/>
                            </Border>
                            <Border Grid.Column="1" BorderBrush="#999" BorderThickness="0.5 0 0.5 0">
                                <TextBlock VerticalAlignment="Center" TextAlignment="Center" Text="{Binding Title}"/>
                            </Border>
                            <Border Grid.Column="2" BorderBrush="#999" BorderThickness="0.5 0 0.5 0">
                                <TextBlock VerticalAlignment="Center" TextAlignment="Center" Grid.Column="2" Text="{Binding Description}"/>
                            </Border>
                            <Border Grid.Column="3" BorderBrush="#999" BorderThickness="0.5 0 1 0">
                                <TextBlock VerticalAlignment="Center" TextAlignment="Center" Grid.Column="3" Text="{Binding Status}"/>
                            </Border>
                            <!--<Grid Grid.Column="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"></Button>
                            <Button Grid.Column="1"></Button>
                            <Button Grid.Column="2"></Button>
                            </Grid>-->
                    </Grid>
                    <DataTemplate.Triggers>
                        <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                            <Setter Property="Background" Value="White" TargetName="ListEl"/>
                        </Trigger>
                        <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                            <Setter Property="Background" Value="WhiteSmoke" TargetName="ListEl"/>
                        </Trigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
       
    </Grid>
    </ScrollViewer>
</UserControl>
