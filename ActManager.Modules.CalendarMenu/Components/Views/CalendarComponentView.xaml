<UserControl x:Class="ActManager.Modules.CalendarMenu.Components.Views.CalendarComponentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ActManager.Modules.CalendarMenu.Components.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:core="clr-namespace:ActManager.Core;assembly=ActManager.Core"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="704" d:DesignWidth="1050"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Заголовок с месяцем и кнопками -->
            <StackPanel Orientation="Horizontal" Margin="10">
                <Button Content="◀" Command="{Binding PreviousMonthCommand}" />
                <TextBlock Text="{Binding CurrentMonth}" FontSize="18" FontWeight="Bold" 
               VerticalAlignment="Center" Margin="10,0"/>
                <Button Content="▶" Command="{Binding NextMonthCommand}" />
            </StackPanel>

            <!-- Таблица с днями недели и днями месяца -->
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Дни недели -->
                <UniformGrid Columns="7">
                    <TextBlock Text="Пн" FontWeight="Bold" TextAlignment="Center"/>
                    <TextBlock Text="Вт" FontWeight="Bold" TextAlignment="Center"/>
                    <TextBlock Text="Ср" FontWeight="Bold" TextAlignment="Center"/>
                    <TextBlock Text="Чт" FontWeight="Bold" TextAlignment="Center"/>
                    <TextBlock Text="Пт" FontWeight="Bold" TextAlignment="Center"/>
                    <TextBlock Text="Сб" FontWeight="Bold" Foreground="Blue" TextAlignment="Center"/>
                    <TextBlock Text="Вс" FontWeight="Bold" Foreground="Red" TextAlignment="Center"/>
                </UniformGrid>

                <!-- Дни месяца -->
                <ItemsControl Grid.Row="1" ItemsSource="{Binding Days}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="7"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="Gray" BorderThickness="0.5" Padding="5" Margin="3">
                                <StackPanel>
                                    <TextBlock Text="{Binding Day}" FontWeight="Bold"/>
                                    <ItemsControl ItemsSource="{Binding Events}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="LightBlue" Padding="2" Margin="2">
                                                    <TextBlock Text="{Binding Title}" FontSize="12"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
