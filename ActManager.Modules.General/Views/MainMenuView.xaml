<UserControl x:Class="ActManager.Modules.General.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ActManager.Modules.General.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:core="clr-namespace:ActManager.Core;assembly=ActManager.Core"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="704" d:DesignWidth="1400"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ActManager.Core;component/Resources/StyleDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid d:Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="208"/>
            <RowDefinition Height="80"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Foreground="#555" Text="Главное меню" FontSize="32" TextAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid Background="WhiteSmoke" Grid.Column="1" Margin="10 0">
                <Grid.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="Black" Opacity="0.6"/>
                </Grid.Effect>
                <ContentControl prism:RegionManager.RegionName="MainMenuItem1"/>
            </Grid>
            <Grid Grid.Column="2" Background="WhiteSmoke" Margin="10 0">
                <Grid.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="Black" Opacity="0.6"/>
                </Grid.Effect>
                <ContentControl prism:RegionManager.RegionName="MainMenuItem2"/>
            </Grid>
            <Grid Grid.Column="3" Background="WhiteSmoke" Margin="10 0">
                <Grid.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="Black" Opacity="0.6"/>
                </Grid.Effect>
                <ContentControl prism:RegionManager.RegionName="MainMenuItem3"/>
            </Grid>
        </Grid>
        <Border Margin="50 10" Grid.Row="3" BorderBrush="#FFCDCDCD" BorderThickness="1" >
            <Grid Margin="2">
                <ContentControl prism:RegionManager.RegionName="MainMenuToDoListRegion"/>
            </Grid>
        </Border>
        
    </Grid>
</UserControl>
