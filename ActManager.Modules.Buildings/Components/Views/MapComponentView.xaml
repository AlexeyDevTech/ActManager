<UserControl x:Class="ActManager.Modules.Buildings.Components.Views.MapComponentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:local="clr-namespace:ActManager.Modules.Buildings.Components.Views"
             xmlns:core="clr-namespace:ActManager.Core;assembly=ActManager.Core"
             xmlns:helpers="clr-namespace:ActManager.Modules.Buildings.Helpers"
             xmlns:vm="clr-namespace:ActManager.Modules.Buildings.Components.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             xmlns:gmap="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
             xmlns:prism="http://prismlibrary.com/"             
             d:DesignHeight="450" d:DesignWidth="800"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <gmap:GMapControl x:Name="MapControl"
                          MapProvider="{Binding MapProvider}"
                          Position="{Binding MapPosition, Mode=TwoWay}"
                          Zoom="{Binding ZoomLevel, Mode=TwoWay}"
                          MaxZoom="19"
                          MinZoom="10"
                          CanDragMap="True"
                          MouseWheelZoomEnabled="True" MouseWheelZoomType="ViewCenter" />
    </Grid>
</UserControl>
