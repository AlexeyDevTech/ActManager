<UserControl x:Class="ActManager.Modules.Buildings.Views.BuildingMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:local="clr-namespace:ActManager.Modules.Buildings.Views"
             xmlns:core="clr-namespace:ActManager.Core;assembly=ActManager.Core"
             xmlns:helpers="clr-namespace:ActManager.Modules.Buildings.Helpers"
             xmlns:vm="clr-namespace:ActManager.Modules.Buildings.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vm:BuildingMenuViewModel}"
             d:DesignHeight="704" d:DesignWidth="1400"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <Style x:Key="SelectableItemStyle" TargetType="ContentPresenter">
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Border x:Name="Border"
                                Background="Transparent"
                                BorderBrush="Gray"
                                BorderThickness="1"
                                Margin="2">
                            <ContentPresenter Content="{Binding}" />
                        </Border>
                        <DataTemplate.Triggers>
                            <Trigger Property="helpers:SelectableItemsControl.IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="LightBlue" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="Blue" />
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid d:Background="#FFE7E9EB">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2.5*"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Border Background="White" Margin="20,20,10,20" CornerRadius="15">
                <helpers:SelectableItemsControl Margin="10" ItemsSource="{Binding Buildings}" SelectedItem="{Binding SelBuilding}" ItemContainerStyle="{StaticResource SelectableItemStyle}">
                    <helpers:SelectableItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="Border"
                                    Background="White"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Margin="5" Height="60">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" Color="#FFE6E6E6" ShadowDepth="3" Direction="270"></DropShadowEffect>
                            </Border.Effect>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Image Source="/ActManager.Modules.Buildings;component/Resources/building-100.png" Width="20" Height="20" />
                                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{Binding Name}" Padding="5" FontSize="18"/>
                                </Grid>
                                
                            </Border>
                            <DataTemplate.Triggers>
                                <Trigger Property="helpers:SelectableItemsControl.IsSelected" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#FFF5F5FF" />
                                    <!--<Setter TargetName="Border" Property="BorderBrush" Value="Blue" />-->
                                </Trigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </helpers:SelectableItemsControl.ItemTemplate>
                </helpers:SelectableItemsControl>
            </Border>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="120"/>
                <RowDefinition Height="*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid>
                <Border Background="White" Margin="10,20,20,10" CornerRadius="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding SelBuilding.AddressInst.Street}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <ContentControl prism:RegionManager.RegionName="BuildingMapRegion"/>
                <Border Grid.Column="1" Background="White" Margin="10,10,20,10" CornerRadius="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelBuilding.Name}"/>
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Number of floors:"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="2"/>
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Construction year:"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="1969"/>
                    </Grid>
                </Border>
            </Grid>
            <Grid Grid.Row="2">
                <Border Background="White" Margin="10,10,20,20" CornerRadius="15">
                    <Grid Margin="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="80"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,20">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Список актов" FontFamily="Tahoma" FontWeight="Bold" Foreground="#333" FontSize="18" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="1" Text="Акты по зданию" FontFamily="Tahoma" Foreground="#999" FontSize="16" VerticalAlignment="Center"/>
                            <Button Grid.Column="4" Grid.Row="0" Grid.RowSpan="2" Height="30">
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Setter Property="Background" Value="#FFE7F0FF"/>
                                        <Setter Property="Margin" Value="5"/>
                                        <Setter Property="BorderThickness" Value="0.5"/>
                                        <Setter Property="BorderBrush" Value="#CCC"/>
                                        <Setter Property="Opacity" Value="0.6"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8"
                                        Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Opacity" Value="0.9"/>
                                                            <Setter Property="BorderThickness" Value="1"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter Property="Opacity" Value="1"/>
                                                            <Setter Property="Background" Value="#FFD3DCEB"/>
                                                        </Trigger>
                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter Property="Opacity" Value="0.2"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                                <Grid>
                                    <TextBlock FontFamily="Tahoma" FontWeight="Bold" Text="Добавить акт" Foreground="#FF465AB9"/>
                                </Grid>
                            </Button>
                        </Grid>
                        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="BuildingActListRegion"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
